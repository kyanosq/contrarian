interface:
  display_name: "Council Review"
  short_description: "Targeted bug hunting with a stop rule"
  default_prompt: "Use $council-review to audit this target until the consecutive-noise stop rule is reached."
